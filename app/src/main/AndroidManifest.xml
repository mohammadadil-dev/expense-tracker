<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required by AdMob to serve ads and check network status -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Daily "Did you log today?" reminder (Android 13+ requires runtime grant) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Allow reminder notification to vibrate the device -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- Re-register AlarmManager alarm after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required for BiometricPrompt on Android 9 and below -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- Voice expense entry — microphone input for speech recognition -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Receipt / Bill camera scan -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Ensure the on-device speech recognizer is available -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".ExpenseApp"
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.ExpenseTracker">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.ExpenseTracker">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            AdMob App ID — REQUIRED even in test mode or the app will crash at startup.
            Replace the test value below with your real App ID from https://admob.google.com
            (Apps → App settings → App ID) before publishing to the Play Store.
        -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-8890346685665889~3572471547" />

        <!--
            Daily reminder BroadcastReceiver.
            Handles ACTION_NOTIFY (from AlarmManager), BOOT_COMPLETED, and MY_PACKAGE_REPLACED
            so alarms survive reboots and app updates.
        -->
        <receiver
            android:name=".util.ReminderReceiver"
            android:exported="false">
            <intent-filter>
                <!-- Fired by AlarmManager.setAlarmClock() at the user's chosen time -->
                <action android:name="com.expensetracker.app.DAILY_REMINDER" />
                <!-- Re-register the alarm after reboot (alarms are cleared on restart) -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- Re-register after app update (alarms are cleared on package replace) -->
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!--
            Home-screen widget.
            ACTION_APPWIDGET_UPDATE is mandatory; APPWIDGET_PROVIDER meta-data points to the
            widget's configuration XML (min size, update period, etc.).
        -->
        <receiver
            android:name=".widget.ExpenseWidgetReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/expense_widget_info" />
        </receiver>

        <!-- Lets us hand a generated PDF report to the system share/save chooser without
             needing any storage permission or leaving the device — purely local file I/O. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
