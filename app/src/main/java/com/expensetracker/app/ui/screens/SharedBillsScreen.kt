package com.expensetracker.app.ui.screens

// Shared Bills now lives inside the Splits tab (the "Shared bills" mode of SplitsScreen), so the
// old standalone groups-list screen is no longer used. Kept as an empty file to avoid a dangling
// reference; the groups list + create-group flow are in SplitsScreen / AddSharedBillGroupSheet.
